# 微信返利机器人依赖包
# 安装命令: pip install -r requirements.txt

# ==================== 核心依赖 ====================
openai>=1.0.0
requests>=2.31.0
colorlog>=6.7.0
pydantic>=2.0.0

# 管理后台 / 调试 API
fastapi>=0.110.0
uvicorn>=0.27.0
itsdangerous>=2.1.0

# wxauto 微信自动化（Windows + PC 微信）
# 免费版 wxauto4，支持 Python 3.12；Python 3.13 需改用 wxautox4（Plus）
wxauto4
uiautomation
pywin32
